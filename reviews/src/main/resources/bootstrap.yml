spring:
  application:
    name: reviews
  cloud:
    nacos:
      discovery:
        server-addr: xxx
        namespace: xxx
      config:
        server-addr: xxx
        namespace: xxx
        name: reviews.yaml
        file-extension: yaml

ratings:
  enabled: false
  color: black

#management:
#  endpoints:
#    web:
#      exposure:
#        include: "*"