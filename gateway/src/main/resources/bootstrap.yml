spring:
  application:
    name: gateway
  cloud:
    nacos:
      discovery:
        server-addr: xxx
        namespace: xxx
      config:
        server-addr: xxx
        namespace: xxx
        name: xxx.yaml
        file-extension: yaml
    gateway:
      routes:[]

management:
  endpoints:
    web:
      exposure:
        include: "*"